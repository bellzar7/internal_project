import React from 'react';
import {UserInfo} from "../userInfo/UserInfo";

const Header = () => {
    return (
        <div>
            headeer
            <UserInfo/>
        </div>
    );
};

export {Header};